#include <bits/stdc++.h>
using namespace std;

// Rearrange function if negative and positive are not n/2
vector<int> rearrange(vector<int> &arr)
{
  int n = arr.size();
  vector<int> ans(n, 0);

    return ans;
}

int main()
{

  vector<int> arr = {1, 2, 3, 1};
  vector<int> ans = rearrange(arr);

  for (int i = 0; i < ans.size(); i++)
  {
    cout << ans[i];
  }
}